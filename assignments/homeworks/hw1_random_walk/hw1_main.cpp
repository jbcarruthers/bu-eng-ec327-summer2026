#include <iostream>

#include "Histogram.h"
#include "Walker.h"

// =============================================================
//  hw1_main.cpp  --  EC327 Summer 2026, Homework 1
//
//  This file orchestrates the experiment. Build it up one Part at a
//  time -- get Part 1 printing before you touch Part 2. The starter
//  compiles and runs as-is; it just does not do anything yet.
// =============================================================

int main() {
  std::cout << "EC327 HW1 -- random walk (starter -- nothing implemented yet)\n";

  // TODO (Part 1): create one Walker at position 0, call step() on it
  //   10 times, and print its position after each step.

  // TODO (Part 2): run three walkers for N = 10, 100, 1000 steps and
  //   print each final position. Run the program a few times -- the
  //   answers change, and that is the point.

  // TODO (Part 3): use run_trials() with K = 10, 100, 1000 (N = 100),
  //   and print the mean and sample variance of each batch. Print each
  //   pair in the form:   mean = X    variance = Y

  // TODO (Part 4): run K = 10000 walkers for N = 100 steps, build a
  //   histogram with bucket_width = 5, and print it. Capture output for
  //   at least three (K, N) combinations into output.txt.

  return 0;
}
