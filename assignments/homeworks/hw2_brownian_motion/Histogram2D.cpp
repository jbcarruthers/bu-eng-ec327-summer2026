#include "Histogram2D.h"

#include <iostream>
#include <map>
#include <vector>

// =============================================================
//  Histogram2D.cpp  --  implementations of the things declared in
//  Histogram2D.h. You write these in Part 4. Most of histogram_int and
//  all of print_histogram can come straight from your HW1 code.
// =============================================================

std::map<int, int> histogram_int(
    [[maybe_unused]] const std::vector<int>& values,
    [[maybe_unused]] int bucket_width) {
  // Part 4
  return {};
}

std::map<int, int> histogram_distance(
    [[maybe_unused]] const std::vector<double>& values,
    [[maybe_unused]] double bucket_width) {
  // Part 4
  return {};
}

void print_histogram([[maybe_unused]] const std::map<int, int>& h) {
  // Part 4
}
